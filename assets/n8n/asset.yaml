apiVersion: asset.sap/v1
kind: Asset

metadata:
  name: n8n
  version: 1.0.0
  description: n8n workflow that detects overdue customers in S/4HANA and orchestrates personalized collection email drafting via the AI agent.

type: n8n-workflow

sourceRoot: "workflows"

provides:
  workflows:
    - definitionFile: collection-email-orchestration.n8n.json
      name: collection-email-orchestration
